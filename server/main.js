import { Meteor } from 'meteor/meteor';
import '../imports/api/templates.js';
import '../imports/api/columns.js';
import '../imports/api/dragCategories.js';
Meteor.startup(() => {
  // code to run on server at startup
});
